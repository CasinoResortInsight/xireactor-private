# Private deployment config

Branded `render.yaml`, `docker-compose.override.yml`, templates, and env samples
specific to the CasinoResortInsight deployment.

For the full overlay layout, sync workflow, and feature-adding playbook, see
[../../PRIVATE.md](../../PRIVATE.md) at the repo root.
